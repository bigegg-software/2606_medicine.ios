import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 172,
    height: 60,
  },
  chart: {
    width: 172,
    height: 60,
  },
});

export default styles;
