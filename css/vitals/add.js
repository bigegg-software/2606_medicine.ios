import { StyleSheet } from 'react-native';
import { AppTheme } from '@/common/theme';

const styles = StyleSheet.create({
    container: { flex: 1, backgroundColor: AppTheme.backgroundColor },
    body: { padding: 18, paddingTop: 2, paddingBottom: 40 },
    sectionTitle: { marginTop: 18, fontWeight: 500, fontSize: 18, marginBottom: 18, color: AppTheme.textPrimary },
    rowBox: {
        paddingHorizontal: 14,
        paddingTop: 2,
        paddingBottom: 14,
        borderRadius: 8,
        marginTop: 12,
        backgroundColor: '#FFFFFF',
        shadowColor: AppTheme.primaryColor,
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.1,
    },
    rowTitle: { marginTop: 12, fontWeight: 400, fontSize: 16, color: AppTheme.textPrimary },
    rowLineInHeader: { height: 1, backgroundColor: '#E8DED2', marginTop: 12 },
    textareaBox: {
        minHeight: 90,
        padding: 12,
        paddingVertical: 8,
        borderRadius: 8,
        backgroundColor: '#F5F8FF',
        fontSize: 14,
        color: AppTheme.textPrimary,
        textAlignVertical: 'top',
    },
    inputBox: { width: 141, height: 34, backgroundColor: "#F5F8FF", textAlign: "center", margin: "auto" },
    typeItem: {
        height: 32,
        paddingHorizontal: 14,
        marginRight: 8,
        backgroundColor: '#FFFFFF',
        borderRadius: 16,
        borderWidth: 1,
        borderColor: 'rgba(102,102,102,0.29)',
        justifyContent: 'center',
        alignItems: 'center',
    },
    typeItemActive: { backgroundColor: '#F5F8FF', borderColor: AppTheme.primaryColor },
    typeItemText: { fontWeight: 400, fontSize: 14, color: AppTheme.textPrimary },
    typeItemTextActive: { color: AppTheme.primaryColor },
    statusRow: { flexDirection: 'row', alignItems: 'center' },
    siteRow: { flexDirection: 'row', justifyContent: 'space-between' },
    siteItem: {
        width: '47%',
        height: 44,
        borderRadius: 8,
        borderWidth: 1,
        backgroundColor: '#FFFFFF',
        borderColor: 'rgba(102,102,102,0.29)',
        justifyContent: 'center',
        alignItems: 'center',
    },
    siteItemActive: { backgroundColor: '#F5F8FF', borderColor: '#053A93' },
    siteItemText: { fontWeight: 400, fontSize: 14, color: AppTheme.textPrimary },
    siteItemTextActive: { color: '#053A93' },
    dateValue: { fontWeight: 400, fontSize: 14, color: AppTheme.textPrimary },
    calendarIcon: { width: 19, height: 19, marginLeft: 8 },
    addBtn: { marginTop: 12, marginHorizontal: 18, height: 45, backgroundColor: '#053A93', borderRadius: 8 },
    addText: { fontWeight: 500, fontSize: 16, color: '#FFFFFF' },
    btmTitle: { marginTop: 14, fontWeight: 400, fontSize: 12, color: AppTheme.textPrimary, textAlign: 'center' },
    btmText: { fontWeight: 400, fontSize: 12, color: AppTheme.textSecondary, textAlign: 'center' },
});

export default styles;
